// This script will be loaded in the webview context
window.addEventListener("DOMContentLoaded", () => {
  console.log("Webview preload script loaded");

  // You can add any custom functionality for the webview here
  // For example, you might want to expose certain APIs to the webview
});
